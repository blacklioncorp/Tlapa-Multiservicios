# Esta URL será reemplazada por Render automáticamente en producción.
# Para desarrollo local, deberás instalar PostgreSQL y poner aquí tu cadena de conexión.
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"
